//@version=6
library("zone")

export gap(int lookback) =>
	for var int i = 0 to lookback
		var int i_fst = i + 1

		if open[i] != open[i_fst]
			box.new(left = bar_index[i_fst], right = bar_index[i], bottom = open[i_fst], top = open[i])

			break
