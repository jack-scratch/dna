//@version=6
indicator("High", overlay = true)

var int lookback = input.int(20, title = "Lookback")

const offset = ta.highestbars(high, lookback)

if barstate.islast
    label.new(x = bar_index + offset, y = high[-offset], text = "Highest High")
