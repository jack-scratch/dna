//@version=6
library("doji")

export doji(int lookback) =>
	var int idx = -1
	for var int i = 0 to lookback
		if close[i] == open[i]
			idx := i	

			break

	if idx != -1
		label.new(bar_index - idx, high[idx], "Doji Candle")
