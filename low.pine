//@version=6
indicator("Low", overlay = true)

lookback = input.int(20, title = "Lookback")

offset = ta.lowestbars(low, lookback)

if barstate.islast
	label.new(x = bar_index + offset, y = low[-offset], text = "Lowest Low")
