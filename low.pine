//@version=6
indicator("Low", overlay = true)

var int lookback = input.int(20, title = "Lookback")

const offset = ta.lowestbars(low, lookback)

if barstate.islast
	label.new(x = bar_index + offset, y = low[-offset], text = "Lowest Low")
