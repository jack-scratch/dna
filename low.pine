//@version=6
indicator("Low (L)", overlay = true)

var int lookback = input.int(100, title = "Lookback")

int offset = ta.lowestbars(low, lookback)

if barstate.islast
	label.new(x = bar_index + offset, y = low[-offset], text = "Low (L)")
